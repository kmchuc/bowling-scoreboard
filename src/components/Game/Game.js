import React from 'react';
import Frames from '../Frames/Frames';

import {
   GameContainer
} from './styled';

const Game = () => {
   return(
       <GameContainer>
           <Frames/>
        </GameContainer>
   )
}

export default Game;